from custom_gmwfs.typing import GroupElementT
from rngcontext import RngContext


def sample_group(rng: RngContext) -> GroupElementT:
    # Generate a random group element based on a given rng
    return 0


def sample_set(rng: RngContext) -> GroupElementT:
    # Generate a random set element based on a given rng
    return 0
